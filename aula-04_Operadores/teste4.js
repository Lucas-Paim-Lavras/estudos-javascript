let num1=70, num2=9, res=0 //preciso declarar as duas separadamente só que na mesma linha

num1=3
num1-- //decremento (tira 1)
num1++ //incremento (acrescenta 1)
num1+=4 //Mesmo += 1 do python
num1*=3 // repete pra qualquer operador
num1/=7
num2 = 8**3
console.log(num1)
console.log(num2)

/*🧮 Aritméticos
    + soma
    - subtração
    * multiplicação
    / divisão
    % resto da divisão
    ** exponenciação
    ++ incremento
    -- decremento

🧪 Comparação
    == igual (com coerção de tipo)
    === estritamente igual (sem coerção)
    != diferente
    !== estritamente diferente
    > maior que
    < menor que
    >= maior ou igual
    <= menor ou igual

🧠 Lógicos
    && E (and)
    || OU (or)
    ! NÃO (not)

📦 Atribuição
    = atribuição simples
    +=, -=, *=, /=, %= e **= → combina com operação

🔎 Outros úteis
    typeof → tipo da variável
    instanceof → verifica instância de objeto
    ? : → operador ternário (condição ? valor1 : valor2)
    , → operador vírgula (executa múltiplas expressões)
    delete → remove propriedade de objeto
    in → verifica se uma propriedade existe em um objeto
    
    */
