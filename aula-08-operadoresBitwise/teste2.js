let n1=10
let n2=11

let resultado = n1 | n2  //Bitwise OR que ensina a tabela. Bsicamente verifica se há equivalência entre os números. Ele apenas retorna 1 se tivermos 1 na comparação se não houver (apenas 0) ele retorna 0. Ver a tabela de verdade.
//n1 e n2 são convertidos para binário e depois comparados. O resultado é convertido de volta para decimal.


console.log(resultado)